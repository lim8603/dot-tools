# 의사결정 권한 매트릭스 (Decision Authority Matrix)

> 누가 무엇을 결정하는가 — Human/AI/Joint 의사결정 경계 정의

---

## 목적

AI와 Human이 협업할 때, 모든 결정에 동일한 수준의 검증이 필요한 것은 아니다.
이 문서는 의사결정의 권한 수준을 명확히 하여 **불필요한 대기를 줄이고, 중요한 결정에 집중**할 수 있도록 한다.

---

## 권한 수준 (Authority Levels)

| Level | 이름 | 설명 |
|-------|------|------|
| **H** | Human Decides | Human이 최종 결정. AI는 옵션과 근거를 제시 |
| **J** | Joint Decision | AI가 제안하고 Human이 검토·승인 후 진행 |
| **A** | AI Autonomous | AI가 자율 판단하여 진행. 사후 리뷰 가능 |

---

## 의사결정 매트릭스 (Decision Matrix)

| 영역 | 결정 사항 | Level | 비고 |
|------|----------|-------|------|
| **아키텍처** | 핵심 기술 스택 선정 | H | |
| | 설계 패턴 선택 | J | AI가 trade-off 분석 제시 |
| | 모듈 내부 구조 | A | 컨벤션 범위 내 |
| **요구사항** | 기능 범위 확정 | H | |
| | 요구사항 상세화 | J | AI가 초안, Human 검증 |
| | 수락 기준 작성 | J | |
| **구현** | 알고리즘/로직 선택 | J | |
| | 변수명/함수 시그니처 | A | 컨벤션 준수 전제 |
| | 에러 핸들링 전략 | J | |
| | 리팩토링 | A | 기능 변경 없는 경우 |
| **테스트** | 테스트 전략 | J | |
| | 테스트 코드 작성 | A | |
| **문서** | 산출물 구조 변경 / 목록 문서 승격 | H | Human 승인 후 진행 |
| | 문서 내용 작성/수정 | A | Human 리뷰 전제 |
| **형상관리** | 브랜치 전략 | H | |
| | 커밋 메시지 | A | 컨벤션 준수 |

---

## 협업 실행 모드 (Collaboration Execution Mode) — F-06

> 역할 좌석의 **정의**와 매 세션 **부기**를 분리하는 운영 스위치. `project_state.md`의 `협업 실행 모드 = solo | team` 필드로 분기한다. (개념 요약은 `cowork.md` §협업 실행 모드)

### 모드별 운영

| 항목 | `team` | `solo` |
|------|--------|--------|
| 좌석 정의(역할 경계·권한·소유 영역) | 유지 | **유지** (미래 온보딩용 스캐폴드) |
| 역할별 `my_state.md` 부기 | 운영 | 생략 (단일 `my_state.md` 또는 project_state 중심) |
| `team_board.md` | 운영 | 미생성 |
| 상향 동기화(개인↔공통) | 운영 | 생략 |
| 의사결정 권한(H/J/A) | 좌석별 | 1인이 전 좌석 겸임(기본 Master) |
| 브리핑 역할 뷰(Master/Member) | 분리 | 단일(§`session_protocol.md` 1인 브리핑) |

### 좌석 정의는 왜 solo에서도 유지하는가

- 좌석 정의(누가 무엇을 결정·소유하는지)는 **미래 동료가 앉을 자리의 청사진**이다. 사람이 합류하면 §`session_protocol.md` 1B-3 매칭으로 그 좌석에 배정하면 되고, 온보딩 비용이 선불된다.
- 반대로 매 세션 역할별 부기는 1인에겐 순 오버헤드다. solo는 정의는 남기고 부기만 끈다.

### 모드 전환

- `solo → team`: 실제 사람이 좌석에 배정될 때. `team_board.md` 생성 → 좌석에 배정 → 해당 역할 Task에 담당자 반영 → 개인 `my_state.md` 생성(§`session_protocol.md` 1B-3).
- `team → solo`: 팀원이 이탈해 1인 운영으로 돌아갈 때. 좌석 정의는 아카이브하지 않고 유지하되 부기를 중단한다.
- 전환은 Human 승인 사항(H)이며 `project_state.md`의 모드 필드와 함께 기록한다.

### 판단 기준

- 1인 프로젝트라도 **동료 합류가 현실적 시야에 있으면** 좌석 정의를 적극적으로 갖춘다(team 정의 + solo 부기가 기본 조합).
- 합류 시야가 막연하면 좌석을 최소로 두거나 단일 Master 좌석만 유지한다 — 쓰지 않을 조직도를 매 세션 관리하지 않는다.

---

## 결정 반전·드리프트 감지 (Decision Reversal & Drift Detection) — F-09

> ADR로 확정한 결정이 **ADR 없이 조용히 뒤집히거나**(reversal), 문서·코드가 결정과 어긋나 있는(drift) 상태를 능동적으로 잡는다. 결정 추적성(원칙 #1·#4)의 방어선이다.

### 왜 필요한가

- 확정 결정이 근거 없이 뒤집히면 뒤 결정들이 무너진 토대 위에 쌓인다. 그러나 반전은 대개 **질문이 우연히 건드릴 때만** 드러난다 — 자동 감지 장치가 없으면 운에 의존한다.
- 이 규칙은 그 운을 **점검 트리거**로 대체한다.

### 반전 감지 (Reversal)

- **트리거**: Phase 전환 시(Pre-Gate Harvest와 함께) 및 릴리즈 전.
- **점검**: `adr_registry.md`의 Accepted 결정을 실제 상태(코드·`tech_stack.md`·설계 기준 본문)와 대조한다. 결정과 다르게 구현/문서화된 항목을 찾는다.
- **조치**: 반전이 실체면 (a) 되돌리거나 (b) **정정 ADR을 채번**해 결정을 갱신한다(무근거 반전을 사후에 정당한 결정으로 승격 또는 롤백). 이력은 commit·세션 로그로 보존(ADR 본문은 현 결정만).

### 결정 코호트 재검토 (Decision Cohort Review)

- 특정 기간·조건에서 내려진 결정이 **체계적으로 의심스러울 근거**가 있으면(예: 알려진 도구 품질 저하 구간, 급하게 몰아친 스프린트), 그 코호트를 재검토 후보로 표시한다.
- 개별 결정이 아니라 **코호트 단위**로 본다 — 한 건이 틀렸다면 같은 창의 다른 결정도 의심한다.
- 재검토 결과는 정상/정정/반전 중 하나로 닫고, 정정·반전은 위 절차를 따른다.

### 미결 위의 분기 (Divergence over an Open Question) — F-12

- 위 두 절이 **결정된 것**의 반전·드리프트를 본다면, 이 절은 **아직 결정되지 않은 것** 위에서 벌어지는 분기를 본다. 열린 Open Question은 문서의 빈칸이 아니다 — 그것을 참조하는 문서 둘 이상이 **서로 다른 답을 임시로 채워 넣고 각자 굳어간다**. 어느 쪽도 ADR을 위반하지 않으므로 F-09의 반전 점검에는 걸리지 않는다.
- **트리거**: Phase 전환 시, 릴리즈 전, 그리고 **열린 OQ가 걸린 영역의 구현 착수 직전**.
- **점검**: 열린 OQ 목록을 훑고, 각 OQ를 참조하거나 그 답에 의존하는 기준 문서·설계 문서·시안이 **같은 답을 전제하고 있는지** 대조한다. 서로 다른 답이 쓰여 있으면 그 OQ는 이미 암묵적으로 두 번 결정된 것이다.
- **조치**: 착수 전에 Human 결정으로 **못 박고**(ADR 채번), 갈라져 있던 문서를 그 결정으로 정렬한다. 분기를 남긴 채 구현하면 어느 쪽 문서를 읽었느냐가 코드를 가르고, 그 위에 쌓인 파생 전제(해시·계약·식별자)까지 함께 무너진다.
- 결정이 아직 이르다고 판단되면 최소한 **분기 사실 자체를 OQ 항목에 기록**해, 다음 세션이 같은 갈림길을 다시 우연에 맡기지 않게 한다.

### 권한

- 반전 여부 판정과 정정 ADR 채번은 **J**(AI 제안 → Human 승인). 무근거 반전을 발견하면 AI는 우려를 1회 표명하고 Human 결정을 따른다.
- 미결 위 분기(F-12)의 해소도 **J** — 분기 사실 보고와 정렬안 제시는 AI, 못 박는 결정은 Human.
